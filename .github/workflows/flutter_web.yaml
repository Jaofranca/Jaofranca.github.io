name: Run Shell Script

on:
  push:
    branches:
      - master  

jobs:
  run-script:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run Shell Script
        run: |
          chmod +x deploy.sh
          ./deploy.sh